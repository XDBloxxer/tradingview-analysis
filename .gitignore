# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Project specific - CRITICAL: Never commit these!
config.yaml
data/cache/
data/exports/
logs/
*.log

# Model archive — kept locally for rollback but never committed.
# Each retrain uploads ml_models/archive/ as a GitHub Actions artifact
# (retention-days: 90), so the history is preserved without bloating git.
ml_models/archive/

# Credential files - Extra protection (should never exist locally, but just in case)
*_credentials.json
*_service_account.json
*.pem
*.key
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Jupyter
.ipynb_checkpoints

# Backup files
*.bak
*.backup
*~
